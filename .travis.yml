language: python
python:
  - "3.6"
  - "3.7"
sudo: false

notifications:
  email: false

install:
  - pip install tox
  - pip install coveralls
  - pip install aiomas
  - pip install tox-travis
  - pip install scipy
  - pip install noise
  - pip install deap
  - pip install networkx

script:
  - tox -e $TOXENV

env:
  - TOXENV=py36
  - TOXENV=docs
  - TOXENV=flake8

after_success:
  - coveralls
