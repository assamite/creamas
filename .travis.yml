language: python
python: 3.5

notifications:
  email: false

#addons:
#  apt:
#    packages:
#    - python-numpy
#    - python-scipy

install
  - sudo apt-get install liblapack3gf libblas3gf
  - pip install -r requirements.txt
  - pip install tox
  - pip install coveralls

script:
  - tox

env:
  - TOXENV=py35
  - TOXENV=docs
  - TOXENV=flake8

after_success:
  - coveralls
